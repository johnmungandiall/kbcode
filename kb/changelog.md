# Changelog — notable changes, newest first.

The ONLY place release history lives (don't duplicate it in other notes).

## Unreleased
- Knowledge base scaffolded with full project notes.

## v1.4.2 (current)
- Current release. See `kbcode/__init__.py:9`.
