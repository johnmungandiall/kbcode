"""kbcode — a small AI coding agent.

It blends three ideas:
  - Claude Code : an agent loop that reads/writes files and runs commands.
  - Hermes      : persistent memory + learned skills across sessions.
  - claude-kb   : a token-cheap knowledge base so we don't re-scan the repo.
"""

__version__ = "1.6.2"
